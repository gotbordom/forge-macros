{
  "name": "@forge-macros/forge-app",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "pnpm exec tsc -p tsconfig.json",
    "lint": "pnpm exec eslint \"src/**/*.{ts,tsx}\"",
    "typecheck": "pnpm exec tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@forge/api": "^4.0.0",
    "@forge/resolver": "^1.6.0",
    "@forge/ui": "^1.9.0",
    "@forge-macros/types": "workspace:*",
    "@forge-macros/ui-kit": "workspace:*",
    "@forge-macros/utils": "workspace:*"
  }
}
